apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
commonLabels:
  environment: latest
# Prefix/Suffix required for VaultAuth configuration
#
namePrefix: qcon-web-
nameSuffix: -latest
# Teach kustomize how to update generated names
#
configurations:
  - kustomizeconfig.yaml
commonAnnotations:
  version: 1.16.11-d829eb94--2024-09-23.1727105863 # {"$imagepolicy": "qcon:qcon-web-image-policy-latest:tag"}
images:
  - name: "qcon-web"
    newName: "registry.ltc.bcit.ca/web-apps/qcon/qcon-web"
    newTag: "1.16.11-d829eb94--2024-09-23.1727105863" # {"$imagepolicy": "qcon:qcon-web-image-policy-latest:tag"}
# Patches ingress with "latest--" and deployment with annotations
#
patches:
  - target:
      kind: Ingress
    path: ingress-patch.yaml
  - target:
      kind: Deployment
    path: deployment-patch.yaml
  # Incorporate vault-secrets
  #
  # adds `-latest` suffix to VaultAuth `spec.mount`
  - target:
      kind: VaultAuth
    path: vault-secrets-latest/vault-auth-patch.yaml
components:
  # Include environment-specific secrets
  - ./vault-secrets-latest
  # Required for VaultAuth role and serviceaccounts
  - ../../base/components/vault-auth
