apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: qcon-web
  template:
    metadata:
      annotations:
        config.linkerd.io/skip-outbound-ports: "443"
    spec:
      containers:
        - name: qcon-web

